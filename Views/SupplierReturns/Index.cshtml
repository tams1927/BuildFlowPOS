@model HardwareManagementSystem.ViewModels.PagedResult<HardwareManagementSystem.Models.SupplierReturnHeader>
@{
    ViewData["Title"] = "Supplier Returns";
}

<div class="page-header-row mb-4">
    <div>
        <h2 class="page-section-title">Supplier Returns</h2>
        <p class="text-muted mb-0">Return damaged or incorrect goods to suppliers for credit or replacement.</p>
    </div>
    <a asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-lg me-1"></i> New Return</a>
</div>

<div class="hb-card">
    <div class="hb-card-body p-0">
        <table class="table align-middle mb-0">
            <thead class="table-light">
                <tr>
                    <th>Return #</th>
                    <th>Date</th>
                    <th>Supplier</th>
                    <th>Item</th>
                    <th>Base Qty</th>
                    <th>Status</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var h in Model.Items)
                {
                    var d = h.Details.FirstOrDefault();
                    <tr>
                        <td class="fw-semibold">@h.ReturnNumber</td>
                        <td class="small">@h.ReturnDate.ToString("MMM dd, yyyy")</td>
                        <td>@h.Supplier?.SupplierName</td>
                        <td class="small">@(d?.Item?.ItemName ?? "—")</td>
                        <td>@((d?.BaseQuantity ?? 0).ToString("0.###"))</td>
                        <td><span class="badge badge-soft-secondary">@h.Status</span></td>
                        <td class="text-end">
                            <a asp-action="Details" asp-route-id="@h.Id" class="btn btn-sm btn-outline-primary">View</a>
                        </td>
                    </tr>
                }
                @if (!Model.Items.Any())
                {
                    <tr><td colspan="7" class="text-center text-muted py-4">No supplier returns yet.</td></tr>
                }
            </tbody>
        </table>
    </div>
</div>
